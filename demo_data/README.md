Demo data (synthetic)

This folder contains **non-sensitive synthetic submissions** for end-to-end testing.

- These files are NOT real student work.
- Use them to validate that scripts run and produce outputs without requiring private datasets.

Layout
- demo_data/dataset_expanded/ : a minimal expanded-folder layout (similar to the private raw export)
- demo_data/dataset_clean/    : generated by normalize_dataset.py
